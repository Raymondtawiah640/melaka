import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.melakah.app',
  appName: 'Melakah',
  webDir: 'dist/melaka/browser'  // point to your Angular build folder
};

export default config;
